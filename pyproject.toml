[project]
name = "qiprof"
version = "0.1.0"
description = "Quantum-interference-inspired surface profilometry simulations"
requires-python = ">=3.9"
license = {text = "MIT"}

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
